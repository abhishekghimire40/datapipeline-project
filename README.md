# Simple Data pipeline Project

a simple app where we can add an excel file then the app will clean the data, transform it and load it into the database and use that clean data for visualization in the frontend and generating reports out of it
